#Day 3
Today I'v been learned what is "rwx" 
'r' means read equals 4
'w' means write equals 2
'x' means excute equals 1
there were a sign usually view as '-' or 'd'
'-' means normal file
'd' means directory
so there is an example "drwxr-xr-x", it means that objetc was a dhas permission to r w x, group user only just r and x, others as same as group user
and the permission data equals 755

there were two commands I have learned today
"chmod" could using to adjust file permissions such as "chmod 644 test.txt"
"chown" using to exchange file owner, such as "sudo chown root:root test.txt" changing owner and group itself to root
root
