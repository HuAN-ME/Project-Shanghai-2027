apiVersion: v1
kind: Service
metadata:
  name: {{ include "day56-demo.fullname" . }}
  labels:
    {{- include "day56-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "day56-demo.selectorLabels" . | nindent 4 }}
