apiVersion: v1
kind: Service
metadata:
  name: {{ include "company-app.fullname" . }}
  labels:
    {{- include "company-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "company-app.selectorLabels" . | nindent 4 }}
