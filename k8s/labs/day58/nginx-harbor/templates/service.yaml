apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-harbor.fullname" . }}
  labels:
    {{- include "nginx-harbor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-harbor.selectorLabels" . | nindent 4 }}
